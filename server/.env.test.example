# ============================================================================
# Lunara .env.test.example
# Copy this file to `.env.test` for running automated tests.
# ============================================================================
# Environment indicator
NODE_ENV=test

# Port for test server (0 = random free port, avoids conflicts)
PORT=0

# Test database (separate from dev DB to keep data isolated)
# Format: postgres://USER:PASSWORD@HOST:PORT/DB_NAME
DB_URL=postgres://username:password@localhost:5432/lunara_test

# Some tools expect DATABASE_URL too — keep them aligned
DATABASE_URL=postgres://username:password@localhost:5432/lunara_test

# JWT secret for signing tokens in test runs
# Can be simpler/shorter than production
JWT_SECRET=super-secret-test

# Service stubs / test doubles
# Override to point at mock servers or local test instances
TAROT_API_BASE=http://tarot.test.local
MOON_API_URL=http://moon.test.local
MOON_API_BASE=http://moon.test.local

# Allow the frontend test client
CORS_ORIGIN=http://localhost:5173
